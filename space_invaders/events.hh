#pragma once

/*------------------------------------------------------------------------------------------------*/

enum class kind {
  key_up,
  key_down,
  other
};

enum class event {
  coin,
  left,
  right,
  fire,
  player1,
  quit,
  none,
  end
};

/*------------------------------------------------------------------------------------------------*/
